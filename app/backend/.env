# Development environment variables for backend
# NOTE: These values are reasonable defaults for local development and
# for use with the included docker-compose. Update them for your setup.

# SQLAlchemy / DATABASE
# Example for docker-compose (Postgres service named `db`):
DATABASE_URL=postgresql+psycopg2://recipe_user:recipe_pass@localhost:5432/recipe_db
# If you prefer a local sqlite dev DB, you can use:
# DATABASE_URL=sqlite:///./dev.db

# Uvicorn host/port
HOST=0.0.0.0
PORT=8000

# SQLAlchemy echo (true/false) - set to true for verbose SQL logging
SQLALCHEMY_ECHO=false

# App secret key (used later for sessions/auth). Keep secret in production.
SECRET_KEY=49a702cd1d9dbe68db9de6733b79c2cf115097a0aecfe4bba8f47e4c0862491b
ACCESS_TOKEN_EXPIRE_MINUTES=30